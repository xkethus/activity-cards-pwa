import {
  FolderOpen,
  PlusCircle,
  Settings,
  BadgeCheck,
  LogOut,
  LogIn,
} from "lucide-react";

export const Icons = {
  FolderOpen,
  PlusCircle,
  Settings,
  BadgeCheck,
  LogOut,
  LogIn,
};
