import {
  FolderOpen,
  PlusCircle,
  Settings,
  BadgeCheck,
  LogOut,
  LogIn,
  CalendarRange,
  BookOpen,
} from "lucide-react";

export const Icons = {
  FolderOpen,
  PlusCircle,
  Settings,
  BadgeCheck,
  LogOut,
  LogIn,
  CalendarRange,
  BookOpen,
};
